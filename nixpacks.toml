[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["rm -rf apps/web/node_modules/.vite", "npm install"]

[phases.build]
cmds = ["npm run build --workspace=apps/api"]

[start]
cmd = "npm run migrate --workspace=packages/db && node apps/api/dist/apps/api/src/index.js"
